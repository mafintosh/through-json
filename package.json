{
  "name": "through-json",
  "version": "1.1.0",
  "description": "Through stream that parses each write as a JSON message.",
  "repository": "git://github.com/mafintosh/through-json.git",
  "keywords": [
    "stream",
    "streams2",
    "through",
    "json",
    "parse"
  ],
  "dependencies": {
    "through2": "^0.5.1"
  },
  "devDependencies": {
    "tap": "^0.4.8"
  },
  "scripts": {
    "test": "tap test.js"
  }
}
