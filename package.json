{
  "name": "through-json",
  "version": "0.0.0",
  "description": "Through2 stream that parses json messages",
  "dependencies": {
    "through2": "^0.4.1"
  },
  "devDependencies": {
    "tap": "^0.4.8"
  },
  "scripts": {
  	"test": "tap test.js"
  }
}
